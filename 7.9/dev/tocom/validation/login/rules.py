from ttapi import aenums
from basis_validation.utils import compare

#def capability_enum_TT_SUPPORTS_ORDER_QUANTITY_AT_DEPTH_is_False(action, before, after):
#    compare(after.price_callbacks[0].serverCapabilities.Get(aenums.TT_SUPPORTS_ORDER_QUANTITY_AT_DEPTH), False)